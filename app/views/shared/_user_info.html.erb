<%= link_to gravatar_for(current_user, size: Settings.picture.size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("views.shared.profile_link"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t("views.shared.micropost") %></span>
